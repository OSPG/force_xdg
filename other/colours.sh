#!/bin/sh
#
# just a list of common programs with support
# for colours
#
# doesn't get installed by default
#

alias ls="ls --color='auto'"
alias tree="tree -C"
alias grep="grep --color=auto"
alias ip="ip -c"
alias diff="diff --color"
